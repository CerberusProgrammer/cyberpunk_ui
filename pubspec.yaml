name: cyberpunk_ui
description: "Paquete de componentes cyberpunk."
publish_to: 'none'
version: 0.2.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
